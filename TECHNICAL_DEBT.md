# 当前技术债

- `InMemoryJobRepository`：服务重启后任务状态丢失，多实例无法共享。
- `LocalFileStorage`：仅适合单机，缺少对象存储、生命周期清理和磁盘配额管理。
- 后台任务：仍使用 FastAPI `BackgroundTasks`，缺少任务取消、独立 worker 和任务级总超时。
- OCR：`use_ocr_fallback` 目前只是预留参数，未接入真实 OCR 引擎。
- 扫描件预处理：缺少旋转检测、倾斜矫正、裁边和模糊质量评估。
- 手机照片：未实现透视矫正。
- JPEG/WEBP：已有接入代码，但缺少独立自动测试和真实模型验收。
- 混合 PDF：当前按整份文件是否存在文本层分类，尚未逐页识别扫描页。
- CORS/鉴权：未配置跨域白名单、用户鉴权、限流和配额。
- 可观测性：缺少结构化日志、指标、调用费用与供应商 request ID 持久化。
- 模型稳定性：长文档合并仍依赖单次模型输出，需继续加强分层合并和失败恢复。
- 测试基础设施：Starlette `TestClient` 有第三方弃用 warning，后续需升级测试客户端。
